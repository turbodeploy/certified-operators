package com.vmturbo.extractor.schema.json.common;

/**
 * Constants used in data extraction schema.
 */
public class Constants {
    private Constants() {
    }

    /**
     * The timestamp pattern used for timestamp fields in data exporter.
     */
    public static final String TIMESTAMP_PATTERN = "yyyy-MM-dd'T'HH:mm:ss'Z'";
}
