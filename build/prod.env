# Note that these environment configurations are applied in production environments.
# During the build process, this file is renamed to .env on the host machine running XL.
# See https://docs.docker.com/compose/environment-variables/ for more on the docker-compose
# .env file.
######################

######################
# METRON (machine learning) enabled. Defaults to false. Set to true in your shell to override.
# You can also use the metron.py script to start Metron components.
METRON_ENABLED=false