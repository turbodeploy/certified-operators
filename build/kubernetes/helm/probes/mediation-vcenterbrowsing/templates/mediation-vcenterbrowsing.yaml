apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-vcenterbrowsing
  name: mediation-vcenterbrowsing
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-vcenterbrowsing
  name: mediation-vcenterbrowsing
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        zone: secure
        io.kompose.service: mediation-vcenterbrowsing
    spec:
      containers:
      - name: {{ .Chart.Name }}
        image: {{ template "vcenterbrowsing_image" . }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        env:
        - name: JAVA_OPTS
          value: {{ .Values.java.options }}
        - name: LOG4J_CONTEXT_SELECTOR
          value: com.vmturbo.mediation.common.context.ThreadGroupPrefixContextSelector
        - name: clustermgr_host
          value: clustermgr
        - name: clustermgr_port
          value: "8080"
        - name: clustermgr_retry_delay_sec
          value: "10"
        - name: component_type
          value: mediation-vcenterbrowsing
        - name: consul_host
          value: consul
        - name: instance_id
          value: mediation-vcenterbrowsing-1
        ports:
        - containerPort: 8080
          name: vcenter-port
        readinessProbe:
          httpGet:
            port: 8080
            path: /health
        resources:
{{ toYaml .Values.resources | indent 12 }}
        securityContext:
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - mountPath: /home/turbonomic/data
          name: mediation-vcenterbrowsing
        - mountPath: /tmp
          name: mediation-vcenterbrowsing-tmpfs0
      imagePullSecrets:
      - name: turbocred
      restartPolicy: Always
      securityContext:
        fsGroup: 2000
      volumes:
      - name: mediation-vcenterbrowsing
        persistentVolumeClaim:
          claimName: mediation-vcenterbrowsing
      - emptyDir:
          medium: Memory
        name: mediation-vcenterbrowsing-tmpfs0
---
apiVersion: v1
kind: Service
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-vcenterbrowsing
  name: mediation-vcenterbrowsing
spec:
  ports:
  - name: mediation-vcenterbrowsing
    port: 8080
{{- if .Values.global }}
  {{- if .Values.global.debug }}
  - name: mediation-vcenterbrowsing-debug
    port: 8000
  {{- end -}}
{{- end }}
  selector:
    io.kompose.service: mediation-vcenterbrowsing
