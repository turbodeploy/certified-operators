apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-appdynamics
  name: mediation-appdynamics
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-appdynamics
  name: mediation-appdynamics
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        zone: secure
        io.kompose.service: mediation-appdynamics
    spec:
      containers:
      - name: {{ .Chart.Name }}
        image: {{ template "appdynamics_image" . }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        env:
        - name: JAVA_OPTS
          value: {{ .Values.java.options }}
        - name: LOG4J_CONTEXT_SELECTOR
          value: com.vmturbo.mediation.common.context.ThreadGroupPrefixContextSelector
        - name: clustermgr_host
          value: clustermgr
        - name: clustermgr_port
          value: "8080"
        - name: clustermgr_retry_delay_sec
          value: "10"
        - name: component_type
          value: mediation-appdynamics
        - name: consul_host
          value: consul
        - name: instance_id
          value: mediation-appdynamics-1
        readinessProbe:
          httpGet:
            port: 8080
            path: /health
        resources:
{{ toYaml .Values.resources | indent 12 }}
        securityContext:
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - mountPath: /home/turbonomic/data
          name: mediation-appdynamics
        - mountPath: /tmp
          name: mediation-appdynamics-tmpfs0
      imagePullSecrets:
      - name: turbocred
      restartPolicy: Always
      securityContext:
        fsGroup: 2000
      volumes:
      - name: mediation-appdynamics
        persistentVolumeClaim:
          claimName: mediation-appdynamics
      - emptyDir:
          medium: Memory
        name: mediation-appdynamics-tmpfs0
---
apiVersion: v1
kind: Service
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-appdynamics
  name: mediation-appdynamics
spec:
  ports:
  - name: mediation-appdynamics
    port: 8080
{{- if .Values.global }}
  {{- if .Values.global.debug }}
  - name: mediation-appdynamics-debug
    port: 8000
  {{- end -}}
{{- end }}
  selector:
    io.kompose.service: mediation-appdynamics
