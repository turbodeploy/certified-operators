apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-awscost
  name: mediation-awscost
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-awscost
  name: mediation-awscost
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        zone: secure
        io.kompose.service: mediation-awscost
    spec:
      containers:
      - name: {{ .Chart.Name }}
        image: {{ template "awscost_image" . }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        env:
        - name: JAVA_OPTS
{{- if .Values.global }}
  {{- if .Values.global.debug }}
          value: {{ .Values.global.dev_options }} {{ .Values.java.options }}
  {{- else }}
          value: {{ .Values.java.options }}
  {{- end }}
{{- else }}
          value: {{ .Values.java.options }}
{{- end }}
        - name: LOG4J_CONTEXT_SELECTOR
          value: com.vmturbo.mediation.common.context.ThreadGroupPrefixContextSelector
        - name: clustermgr_host
          value: clustermgr
        - name: clustermgr_port
          value: "8080"
        - name: clustermgr_retry_delay_sec
          value: "10"
        - name: component_type
          value: mediation-awscost
        - name: consul_host
          value: consul
        - name: instance_id
          value: mediation-awscost-1
        readinessProbe:
          httpGet:
            port: 8080
            path: /health
        resources:
{{ toYaml .Values.resources | indent 12 }}
        securityContext:
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - mountPath: /home/turbonomic/data
          name: mediation-awscost
        - mountPath: /tmp
          name: mediation-awscost-tmpfs0
      restartPolicy: Always
      securityContext:
        fsGroup: 2000
      imagePullSecrets:
      - name: turbocred
      volumes:
      - name: mediation-awscost
        persistentVolumeClaim:
          claimName: mediation-awscost
      - emptyDir:
          medium: Memory
        name: mediation-awscost-tmpfs0
---
apiVersion: v1
kind: Service
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-awscost
  name: mediation-awscost
spec:
  ports:
  - name: mediation-awscost
    port: 8080
{{- if .Values.global }}
  {{- if .Values.global.debug }}
  - name: mediation-awscost-debug
    port: 8000
  {{- end -}}
{{- end }}
  selector:
    io.kompose.service: mediation-awscost
