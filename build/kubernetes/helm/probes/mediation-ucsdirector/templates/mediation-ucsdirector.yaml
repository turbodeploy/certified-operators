apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-ucsdirector
  name: mediation-ucsdirector
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        zone: secure
        io.kompose.service: mediation-ucsdirector
    spec:
      containers:
      - name: {{ .Chart.Name }}
        image: {{ template "ucsdirector_image" . }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        env:
        - name: JAVA_OPTS
          value: {{ .Values.java.options }}
        - name: LOG4J_CONTEXT_SELECTOR
          value: com.vmturbo.mediation.common.context.ThreadGroupPrefixContextSelector
        - name: clustermgr_host
          value: clustermgr
        - name: clustermgr_port
          value: "8080"
        - name: clustermgr_retry_delay_sec
          value: "10"
        - name: component_type
          value: mediation-ucsdirector
        - name: consul_host
          value: consul
        - name: instance_id
          value: mediation-ucsdirector-1
        readinessProbe:
          httpGet:
            port: 8080
            path: /health
        resources:
{{ toYaml .Values.resources | indent 12 }}
        securityContext:
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - mountPath: /tmp
          name: mediation-ucsdirector-tmpfs0
      imagePullSecrets:
      - name: turbocred
      restartPolicy: Always
      volumes:
      - emptyDir:
          medium: Memory
        name: mediation-ucsdirector-tmpfs0
---
apiVersion: v1
kind: Service
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-ucsdirector
  name: mediation-ucsdirector
spec:
  ports:
  - name: mediation-ucsdirector
    port: 8080
{{- if .Values.global }}
  {{- if .Values.global.debug }}
  - name: mediation-ucsdirector-debug
    port: 8000
  {{- end -}}
{{- end }}
  selector:
    io.kompose.service: mediation-ucsdirector
