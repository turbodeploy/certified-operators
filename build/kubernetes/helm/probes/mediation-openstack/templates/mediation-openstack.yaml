apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-openstack
  name: mediation-openstack
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-openstack
  name: mediation-openstack
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        zone: secure
        io.kompose.service: mediation-openstack
    spec:
      containers:
      - name: {{ .Chart.Name }}
        image: {{ template "openstack_image" . }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        env:
        - name: JAVA_OPTS
          value: {{ .Values.java.options }}
        - name: LOG4J_CONTEXT_SELECTOR
          value: com.vmturbo.mediation.common.context.ThreadGroupPrefixContextSelector
        - name: clustermgr_host
          value: clustermgr
        - name: clustermgr_port
          value: "8080"
        - name: clustermgr_retry_delay_sec
          value: "10"
        - name: component_type
          value: mediation-openstack
        - name: consul_host
          value: consul
        - name: instance_id
          value: mediation-openstack-1
        readinessProbe:
          httpGet:
            port: 8080
            path: /health
        resources:
{{ toYaml .Values.resources | indent 12 }}
        securityContext:
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - mountPath: /home/turbonomic/data
          name: mediation-openstack
        - mountPath: /tmp
          name: mediation-openstack-tmpfs0
      imagePullSecrets:
      - name: turbocred
      restartPolicy: Always
      securityContext:
        fsGroup: 2000
      volumes:
      - name: mediation-openstack
        persistentVolumeClaim:
          claimName: mediation-openstack
      - emptyDir:
          medium: Memory
        name: mediation-openstack-tmpfs0
---
apiVersion: v1
kind: Service
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-openstack
  name: mediation-openstack
spec:
  ports:
  - name: mediation-openstack
    port: 8080
{{- if .Values.global }}
  {{- if .Values.global.debug }}
  - name: mediation-openstack-debug
    port: 8000
  {{- end -}}
{{- end }}
  selector:
    io.kompose.service: mediation-openstack
