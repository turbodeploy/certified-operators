# Default values for ml-datastore.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: localhost:5000/turbonomic
  tag: BRANCH
  pullPolicy: IfNotPresent

java:
  options: ""

resources:
  limits:
    memory: 512Mi

nodeSelector: {}

tolerations: []

affinity: {}
