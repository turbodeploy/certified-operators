apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    zone: internal
    io.kompose.service: cost
  name: cost
  namespace: turbonomic
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    zone: internal
    io.kompose.service: cost
  name: cost
  namespace: turbonomic
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      creationTimestamp: null
      labels:
        zone: internal
        io.kompose.service: cost
    spec:
      containers:
      - env:
        - name: JAVA_OPTS
          value: JAVA_OPTIONS
        - name: clustermgr_host
          value: clustermgr
        - name: clustermgr_port
          value: "8080"
        - name: clustermgr_retry_delay_sec
          value: "10"
        - name: component_type
          value: cost
        - name: consul_host
          value: consul
        - name: instance_id
          value: cost-1
        - name: kafkaServers
          value: kafka1:9092
        image: localhost:5000/turbonomic/com.vmturbo.cost.component:BRANCH
        imagePullPolicy: IfNotPresent
        name: cost
        resources:
          requests:
            memory: MEMORY_REQUEST
          limits:
            memory: MEMORY_LIMIT
        securityContext:
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - mountPath: /home/turbonomic/data
          name: cost
        - mountPath: /tmp
          name: cost-tmpfs0
      restartPolicy: Always
      imagePullSecrets:
      - name: turbocred
      securityContext:
        fsGroup: 2000
      volumes:
      - name: cost
        persistentVolumeClaim:
          claimName: cost
      - emptyDir:
          medium: Memory
        name: cost-tmpfs0
---
apiVersion: v1
kind: Service
metadata:
  labels:
    zone: internal
    io.kompose.service: cost
  name: cost
  namespace: turbonomic
spec:
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080
  - name: "20032"
    port: 20032
    targetPort: 8000
  - name: "9001"
    port: 9001
    targetPort: 9001
  selector:
    io.kompose.service: cost
