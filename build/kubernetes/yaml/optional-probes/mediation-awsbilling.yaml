apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-awsbilling
  name: mediation-awsbilling
  namespace: turbonomic
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-awsbilling
  name: mediation-awsbilling
  namespace: turbonomic
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        zone: secure
        io.kompose.service: mediation-awsbilling
    spec:
      containers:
      - env:
        - name: JAVA_OPTS
          value: JAVA_OPTIONS
        - name: clustermgr_host
          value: clustermgr
        - name: clustermgr_port
          value: "8080"
        - name: clustermgr_retry_delay_sec
          value: "10"
        - name: component_type
          value: mediation-awsbilling
        - name: consul_host
          value: consul
        - name: instance_id
          value: mediation-awsbilling-1
        image: localhost:5000/turbonomic/com.vmturbo.mediation.aws.billing.component:BRANCH
        imagePullPolicy: IfNotPresent
        name: mediation-awsbilling
        readinessProbe:
          httpGet:
            port: 8080
            path: /health
        resources:
          requests:
            memory: MEMORY_REQUEST
          limits:
            memory: MEMORY_LIMIT
        securityContext:
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - mountPath: /home/turbonomic/data
          name: mediation-awsbilling
        - mountPath: /tmp
          name: mediation-awsbilling-tmpfs0
      restartPolicy: Always
      securityContext:
        fsGroup: 2000
      imagePullSecrets:
      - name: turbocred
      volumes:
      - name: mediation-awsbilling
        persistentVolumeClaim:
          claimName: mediation-awsbilling
      - emptyDir:
          medium: Memory
        name: mediation-awsbilling-tmpfs0
---
apiVersion: v1
kind: Service
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-awsbilling
  name: mediation-awsbilling
  namespace: turbonomic
spec:
  ports:
  - name: "awsbilling-port"
    port: 8080
    targetPort: 8080
  - name: "awsbilling-debug-port"
    port: 20131
    targetPort: 8000
  selector:
    io.kompose.service: mediation-awsbilling
