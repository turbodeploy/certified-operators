apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-ucsdirector
  name: mediation-ucsdirector
  namespace: turbonomic
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-ucsdirector
  name: mediation-ucsdirector
  namespace: turbonomic
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        zone: secure
        io.kompose.service: mediation-ucsdirector
    spec:
      containers:
      - env:
        - name: JAVA_OPTS
          value: JAVA_OPTIONS
        - name: clustermgr_host
          value: clustermgr
        - name: clustermgr_port
          value: "8080"
        - name: clustermgr_retry_delay_sec
          value: "10"
        - name: component_type
          value: mediation-ucsdirector
        - name: consul_host
          value: consul
        - name: instance_id
          value: mediation-ucsdirector-1
        image: localhost:5000/turbonomic/com.vmturbo.mediation.ucsdirector.component:BRANCH
        imagePullPolicy: IfNotPresent
        name: mediation-ucsdirector
        readinessProbe:
          httpGet:
            port: 8080
            path: /health
        resources:
          requests:
            memory: MEMORY_REQUEST
          limits:
            memory: MEMORY_LIMIT
        securityContext:
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - mountPath: /home/turbonomic/data
          name: mediation-ucsdirector
        - mountPath: /tmp
          name: mediation-ucsdirector-tmpfs0
      imagePullSecrets:
      - name: turbocred
      restartPolicy: Always
      securityContext:
        fsGroup: 2000
      volumes:
      - name: mediation-ucsdirector
        persistentVolumeClaim:
          claimName: mediation-ucsdirector
      - emptyDir:
          medium: Memory
        name: mediation-ucsdirector-tmpfs0
---
apiVersion: v1
kind: Service
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-ucsdirector
  name: mediation-ucsdirector
  namespace: turbonomic
spec:
  ports:
  - name: "ucsdirector-port"
    port: 8080
    targetPort: 8080
  - name: "ucsdirector-port-debug"
    port: 20129
    targetPort: 8000
  selector:
    io.kompose.service: mediation-ucsdirector
