kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: internal-zone-access
spec:
  podSelector:
    matchLabels:
      zone: internal 
  ingress:
    - from:
      - podSelector:
          matchLabels:
            zone: dmz 
      - podSelector:
          matchLabels:
            zone: internal 

