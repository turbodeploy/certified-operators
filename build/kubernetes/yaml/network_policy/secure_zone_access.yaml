kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: secure-zone-access
spec:
  podSelector:
    matchLabels:
      zone: secure
  ingress:
    - from:
      - podSelector:
          matchLabels:
            zone: internal 
      - podSelector:
          matchLabels:
            zone: secure 
