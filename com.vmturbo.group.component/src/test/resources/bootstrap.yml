spring:
  application:
    name: group
  cloud:
    consul:
      enabled: false
      config:
        enabled: false
        fail-fast: false
      discovery:
        enabled: false

server:
  grpcPort: 9001

multipart:
  max-file-size: 100MB
  max-request-size: 100MB
