
<!-- BEGIN-UID.user_ctrls -->

<!-- END-UID.user_ctrls -->

<div  id="add_new_to_members" style="float:right; width:60%;">
    <% dest = url_for :controller => 'entity_assns', :action => 'update_members', :id => @entity_assn.id %>
    <%= form_tag( :controller => 'entities', :action => 'new', :redirect => dest ) %>
        <%= submit_tag 'Add New...' %>
    </form> <!-- end form_tag -->
</div>

<div id="add_to_members_list">
    <%= form_tag( :controller => 'entity_assns', :action => :update_members, :id => @entity_assn ) %>
        <%= submit_tag 'Add Selected' %>
        <p class="select_list">&nbsp;</p>
        <% for entity in @entities %>
            <p class="select_list"> <%= check_box_tag("entity_ids[]",entity.id) %> <%= entity.sname %> </p>
        <% end %>
        <p class="select_list">&nbsp;</p>
        <%= submit_tag 'Add Selected' %>
    </form> <!-- end form_tag -->
</div>

<!-- BEGIN-UID.user_divs -->

<!-- END-UID.user_divs -->

