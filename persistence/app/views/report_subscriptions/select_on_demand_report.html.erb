
<!-- BEGIN-UID.user_ctrls -->

<!-- END-UID.user_ctrls -->

<div id="select_on_demand_report">
    <%= form_tag( :controller => 'report_subscriptions', :action => :update_on_demand_report, :id => @report_subscription ) %>
       <%= submit_tag 'Select' %>
       <p class="select_list">&nbsp;</p>
       <% for on_demand_report in @on_demand_reports %>
           <p class="select_list"> <%= radio_button_tag("on_demand_report_id",on_demand_report.id) %> <%= on_demand_report.sname %> </p>
       <% end %>
       <p class="select_list">&nbsp;</p>
       <%= submit_tag 'Select' %>
    </form> <!-- end form_tag -->
</div>

<!-- BEGIN-UID.user_divs -->

<!-- END-UID.user_divs -->

